/*
import { Router } from "express";
import { notificationController } from "../controllers/notification.controller";

const routes = Router();

routes.post("/notifications", notificationController.postNotifications)
routes.post("/register", notificationController.registerApp)


export default routes;
*/